language: perl
perl:
  - "5.20"
  - "5.18"
  - "5.14"
  - "5.10"
before_install:
  - cd Brocade-BSC
install:
  - cpanm --quiet --installdeps --notest Test::Perl::Critic
  - cpanm --quiet --installdeps --notest .
script:
  - perl Makefile.PL && make test && prove -l xt
